name: sprinkle
description: A convenient and easy to use, stream-based Flutter architecture
version: 0.0.5
homepage: https://github.com/zaiste/sprinkle

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.23.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

